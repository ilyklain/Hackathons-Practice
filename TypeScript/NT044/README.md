# Notion Hackathon Challenge – Block Parser (TS)

## Overview
Transform markdown-style prefixes into structured data objects representing content blocks.

## Objective
Identify line prefixes to categorize text type and sanitize the output content.

## Analysis
- **Time Complexity**: O(L) where L is line length.
- **Concepts**: String parsing, Object mapping.
