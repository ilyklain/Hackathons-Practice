# Discord Hackathon Challenge – Webhook Validator (JS)

## Overview
Discord Webhooks must be validated client-side to ensure mandatory fields are present before sending the request.

## Objective
Validate that a payload contains a valid `content` string or a non-empty `embeds` array.

## Analysis
- **Time Complexity**: O(1)
- **Concepts**: Data Validation, Type checking.
