# Slack Hackathon Challenge – Entity Parser (JS)

## Overview
Slack needs to parse raw text to find mentions and channels to render them as clickable links.

## Objective
Use Regular Expressions to find words starting with `@` and `#`.

## Analysis
- **Time Complexity**: O(N)
- **Concepts**: Regex, String manipulation.
