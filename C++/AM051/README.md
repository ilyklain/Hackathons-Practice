# AMD Hackathon Challenge – Vector Sum (C++)

## Overview
Perform efficient element-wise summation of large data arrays, simulating SIMD processing.

## Objective
Use `std::vector` and its `reserve` method to optimize memory allocation during bulk processing.

## Analysis
- **Time Complexity**: O(N) where N is vector size.
- **Concepts**: Memory pre-allocation, Vector traversal.
