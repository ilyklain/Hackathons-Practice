# Pinterest Hackathon Challenge – API Validator (Elixir)

## Overview
Perform server-side validation on incoming content assets to ensure data completeness before persistent storage.

## Objective
Use Map accessors and conditional logic to verify the presence of mandatory fields in a data structure.

## Analysis
- **Time Complexity**: O(1)
- **Concepts**: Map manipulation, Result tuples ({:ok, :error}).
